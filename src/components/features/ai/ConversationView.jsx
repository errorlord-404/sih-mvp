import { useEffect, useRef, useState } from 'react';
import { Bot, ImagePlus, Mic, Send, Sparkles, User } from 'lucide-react';
import VoiceButton from './VoiceButton.jsx';
import { useAIConversation } from '../../../context/AIConversationContext.jsx';

const statusCopy = { en: 'Tap the mic or type a message below', hi: 'माइक दबाएं या नीचे संदेश लिखें', mr: 'मायक्रोफोन टॅप करा किंवा संदेश लिहा' };

export default function ConversationView({ compact = false }) {
  const { messages, sendText, voiceState, processing, language, submitImage, error } = useAIConversation();
  const [input, setInput] = useState('');
  const endRef = useRef(null);
  const inputRef = useRef(null);
  const status = statusCopy[language] || statusCopy.en;
  useEffect(() => { endRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [messages, processing, voiceState]);
  const submit = (event) => { event.preventDefault(); if (input.trim()) { sendText(input); setInput(''); } };
  return <div className="flex min-h-0 w-full flex-1 flex-col overflow-hidden rounded-2xl border border-border bg-white shadow-card"><div className="min-h-0 flex-1 space-y-4 overflow-y-auto p-4 sm:p-5">{messages.length ? messages.map((message) => <div key={message.id} className={`flex items-start gap-2.5 ${message.role === 'user' ? 'flex-row-reverse' : ''}`}><div className={`grid size-8 shrink-0 place-items-center rounded-full text-xs font-bold ${message.role === 'user' ? 'bg-primary text-white' : 'bg-emerald-100 text-emerald-800'}`}>{message.role === 'user' ? <User size={15} /> : <Bot size={15} />}</div><div className={`flex max-w-[85%] flex-col ${message.role === 'user' ? 'items-end' : 'items-start'}`}><span className="mb-1 px-1 text-[11px] font-bold text-text-secondary">{message.role === 'user' ? 'You' : 'KisanSathi'}</span><div className={`rounded-2xl px-4 py-3 text-xs leading-relaxed sm:text-sm ${message.role === 'user' ? 'rounded-tr-none bg-primary text-white' : 'rounded-tl-none border border-border bg-surface-muted text-text-primary'}`}>{message.image && <img src={message.image} alt="Uploaded crop" className="mb-2 max-h-48 w-full rounded-xl object-cover" />}<p className="whitespace-pre-wrap">{message.text}</p>{message.via === 'voice' && <span className="mt-2 inline-flex items-center gap-1 text-[10px] opacity-70"><Mic size={11} /> Voice message</span>}{message.citations?.length > 0 && <p className="mt-2 text-[10px] text-text-muted">Sources: {message.citations.map((item) => item.source || item.title).join(', ')}</p>}</div><span className="mt-1 px-1 text-[10px] text-text-muted">{message.time}</span></div></div>) : <div className="grid min-h-48 place-items-center text-center text-sm text-text-secondary"><div><Sparkles className="mx-auto text-primary" size={25} /><p className="mt-3">{processing ? 'Advisor is preparing a response…' : 'Ask about a recorded field observation.'}</p></div></div>}{processing && <div className="flex w-fit items-center gap-2 rounded-xl border border-border bg-surface-muted p-3 text-xs text-text-secondary"><Sparkles size={15} className="animate-spin text-primary" />{language === 'hi' ? 'सोच रहा हूँ…' : language === 'mr' ? 'विचार करत आहे…' : 'Advisor is typing…'}</div>}{error && <p className="rounded-lg bg-red-50 p-3 text-xs text-red-700">{error.message}</p>}<div ref={endRef} /></div>{!compact && <div className="shrink-0 space-y-3 border-t border-border bg-white p-3 sm:p-4"><div className="flex items-center gap-2 sm:gap-3"><VoiceButton /><form onSubmit={submit} className="flex flex-1 items-center gap-2 rounded-2xl border border-border bg-surface-muted/40 px-3 py-1.5"><button type="button" onClick={() => inputRef.current?.click()} aria-label="Upload crop photo" className="grid size-7 shrink-0 place-items-center rounded-lg text-text-secondary hover:bg-emerald-50 hover:text-primary"><ImagePlus size={17} /></button><input ref={inputRef} type="file" accept="image/*" className="hidden" onChange={(event) => { const file = event.target.files?.[0]; if (file) submitImage(file); event.target.value = ''; }} /><input value={input} onChange={(event) => setInput(event.target.value)} placeholder={status} className="min-w-0 flex-1 bg-transparent py-1 text-xs outline-none sm:text-sm" /><button type="submit" disabled={!input.trim() || processing} aria-label="Send message" className="grid size-8 shrink-0 place-items-center rounded-xl bg-primary text-white disabled:opacity-40"><Send size={15} /></button></form></div></div>}</div>;
}
