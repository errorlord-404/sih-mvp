import { useRef, useState } from 'react'
import { Check, ImagePlus, X } from 'lucide-react'
import { useAIConversation } from '../../../context/AIConversationContext.jsx'

const Modal = ({ children }) => <div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-5 shadow-2xl">{children}</div></div>
export function ActionPopups() { const { pendingAction, submitImage, resolveConfirmation } = useAIConversation(); const input = useRef(null); const [preview, setPreview] = useState(null); if (!pendingAction) return null; if (pendingAction.type === 'image_upload') return <Modal><div className="flex items-center justify-between"><h2 className="font-bold">Upload crop photo</h2><button onClick={() => resolveConfirmation(false)} aria-label="Close"><X size={19}/></button></div><p className="mt-2 text-sm text-text-secondary">Choose a clear photo of the affected leaf or plant.</p><button onClick={() => input.current?.click()} className="mt-5 flex h-44 w-full flex-col items-center justify-center rounded-xl border-2 border-dashed border-primary-light bg-primary-50 text-primary">{preview ? <img src={preview} className="h-full w-full rounded-xl object-cover" alt="Crop preview"/> : <><ImagePlus size={28}/><span className="mt-2 text-sm font-semibold">Choose image</span></>}</button><input ref={input} type="file" accept="image/*" className="hidden" onChange={event => { const file = event.target.files?.[0]; if (file) setPreview(URL.createObjectURL(file)) }}/>{preview && <button onClick={() => { const file = input.current?.files?.[0]; if (file) submitImage(file) }} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-bold text-white"><Check size={17}/>Confirm photo</button>}</Modal>; return <Modal><h2 className="font-bold">{pendingAction.title || 'Confirm action'}</h2><p className="mt-2 text-sm leading-6 text-text-secondary">This only records your choice in KisanSathi; it will not control any equipment.</p><div className="mt-5 flex gap-3"><button onClick={() => resolveConfirmation(false)} className="flex-1 rounded-xl border border-border px-4 py-2.5 text-sm font-semibold">Cancel</button><button onClick={() => resolveConfirmation(true)} className="flex-1 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-white">{pendingAction.confirmLabel || 'Confirm'}</button></div></Modal> }
